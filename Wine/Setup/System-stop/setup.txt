wine config setup :
terminal : bash
ps aux | grep wine

// wine's built-in stop command
wineboot -k

// system built-in stop command
sudo kill [id] 
sudo kill -9 [id]

// system built-in stop command
sudo pkill -f [system]
sudo pkill -9 [system] 
